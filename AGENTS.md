# AGENTS.md

## Cursor Cloud specific instructions

This repository (`fitfit`) is currently a blank project with only a README. There is no application code, no dependencies, no build system, and no tests yet.

**Current state:** No services, no package manager, no framework selected. Future agents should re-evaluate setup requirements once application code is added.

**When code is added**, update this section and the VM update script to reflect:
- The chosen package manager and dependency installation command
- How to lint, test, build, and run the application
- Any non-obvious environment requirements or gotchas
